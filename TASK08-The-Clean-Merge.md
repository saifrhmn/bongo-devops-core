Created the feature/system-optimization branch and made 3 small messy commits.

Used:

git merge --squash feature/system-optimization
git commit -m "Add system optimization"
Result

The 3 messy feature commits were successfully combined into one clean commit on the main branch.
